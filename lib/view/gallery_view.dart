import 'package:flutter/material.dart';

class GalleryView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Gallery Page',style: TextStyle(fontSize: 100)),
    );
  }
}